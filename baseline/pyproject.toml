[project]
name = "baseline"
version = "0.1.0"
description = "Baseline benchmark for invoice agent with tool calling"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=2.0.0",
    "psutil>=7.2.2",
    "typer>=0.12.5",
]
